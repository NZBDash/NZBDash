@using GridMvc.Html
@model IEnumerable<NZBDash.Common.Models.ViewModels.NzbGet.NzbGetHistoryViewModel>


<div class="col-md-12">

    //TODO: Sort out this grid

    @Html.Grid(Model).Named("NzbGetHistoryGrid").Columns(columns =>
{
    columns.Add(x => x.Name)
        .Titled("Name")
        .Encoded(false)
        .Sanitized(false)
        .SetWidth(300);

    //columns.Add(x => x.Status).RenderValueAs(x =>
    //{
    //    DateTime date;
    //    DateTime.TryParse(x.ReleaseDate, out date);
    //    return date.ToString(CultureInfo.CurrentCulture.DateTimeFormat.ShortDatePattern);
    //}
    //    ).Titled(Resources.Release_Date);


    columns.Add(x => x.Status)
        .Titled("Status");


    columns.Add(x => x.Category)
        .Titled("Category");

    columns.Add(x => x.FileSize);

}).Sortable().Filterable().WithMultipleFilters().WithPaging(15)

</div>